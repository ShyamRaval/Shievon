---
const featureSets = [
  {
    title: 'Audience-first merchandising',
    points: [
      'Dynamic cohorts tuned for student budgets and young professional taste.',
      'Editorial storefronts, not generic grids—each drop feels cinematic.',
      'Ethical sourcing and transparent partner scorecards.',
    ],
    metric: '1:1',
    metricLabel: 'experience personalization',
  },
  {
    title: 'Velocity commerce rails',
    points: [
      'One-touch checkout, split pay, and local pickup baked in.',
      'Inventory orchestration with live health signals for every SKU.',
      'Seller pods with mentorship, analytics, and fulfillment APIs.',
    ],
    metric: '45%',
    metricLabel: 'faster go-live for microbrands',
  },
  {
    title: 'Community-fueled discovery',
    points: [
      'Signal boosters highlight impact-driven makers and rebel designers.',
      'Cause-led collections align commerce with purpose.',
      'Social proof flows into consultancy + events pillars automatically.',
    ],
    metric: '3x',
    metricLabel: 'engagement lift vs standard marketplaces',
  },
];
---

<section id="marketplace" class="relative bg-night px-4 py-16 sm:px-6 lg:px-8 lg:py-24">
  <div class="mx-auto max-w-6xl space-y-10">
    <div class="space-y-5 text-center">
      <p class="text-sm font-semibold uppercase tracking-[0.3em] text-fire">Commerce Core</p>
      <h2 class="text-3xl font-semibold text-white sm:text-4xl">
        Marketplace first. Every other pillar rides on its flame.
      </h2>
      <p class="mx-auto max-w-3xl text-base text-slate-300">
        E-commerce is 80% of the Shievon DNA. It finances the consultancy offering, powers event collaborations, and
        injects momentum into every community activation. We are obsessing over supply-chain intuition before scaling
        into the remaining pillars.
      </p>
    </div>

    <div class="grid gap-6 lg:grid-cols-3">
      {featureSets.map((feature) => (
        <article class="group flex flex-col rounded-3xl border border-white/5 bg-gradient-to-b from-white/5 to-transparent p-6 text-slate-100 shadow-[0_0_80px_-40px_rgba(248,113,113,0.8)] transition hover:border-fire/40 hover:bg-white/10">
          <div class="flex items-center justify-between">
            <h3 class="text-xl font-semibold text-white">{feature.title}</h3>
            <span class="text-sm uppercase tracking-[0.3em] text-fire/80">Commerce</span>
          </div>
          <ul class="mt-4 flex-1 space-y-3 text-sm text-slate-300">
            {feature.points.map((point) => (
              <li class="flex gap-3">
                <span class="mt-1 inline-flex h-1.5 w-6 flex-none rounded-full bg-fire/70"></span>
                <span>{point}</span>
              </li>
            ))}
          </ul>
          <div class="mt-6 rounded-2xl border border-white/10 bg-black/30 p-4 text-center">
            <p class="text-3xl font-semibold text-fire">{feature.metric}</p>
            <p class="text-xs uppercase tracking-[0.3em] text-slate-400">{feature.metricLabel}</p>
          </div>
        </article>
      ))}
    </div>
  </div>
</section>

