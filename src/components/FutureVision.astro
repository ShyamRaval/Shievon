---
const layers = [
  {
    title: 'Phoenix Edge',
    description: 'Static-first experiences rendered via Astro, streaming content islands when needed.',
  },
  {
    title: 'Service Mesh',
    description: 'Headless commerce core + career graph APIs + event orchestration microservices.',
  },
  {
    title: 'Data Flame',
    description: 'Telemetry lake unifying product, talent, and community signals.',
  },
];
---

<section class="relative bg-night px-4 py-16 sm:px-6 lg:px-8 lg:py-24">
  <div class="mx-auto max-w-6xl space-y-10">
    <div class="space-y-4 text-center">
      <p class="text-sm font-semibold uppercase tracking-[0.3em] text-fire">Future Vision</p>
      <h2 class="text-3xl font-semibold text-white sm:text-4xl">Static today. Microservice-native tomorrow.</h2>
      <p class="mx-auto max-w-3xl text-base text-slate-300">
        Shievon will evolve into a massive ecosystem. This landing page is a static foundation, but engineered for
        future backend expansion—from commerce engines to consultancy graphs and social activation services.
      </p>
    </div>

    <div class="grid gap-6 lg:grid-cols-3">
      {layers.map((layer, index) => (
        <article class="flex flex-col rounded-3xl border border-white/5 bg-white/5 p-6 text-slate-200">
          <span class="text-xs uppercase tracking-[0.3em] text-white/70">Layer 0{index + 1}</span>
          <h3 class="mt-4 text-xl font-semibold text-white">{layer.title}</h3>
          <p class="mt-3 text-sm text-slate-300">{layer.description}</p>
          <div class="mt-6 grow rounded-2xl border border-white/10 bg-black/30 p-4">
            <svg viewBox="0 0 220 120" role="img" aria-hidden="true" class="h-28 w-full text-fire/60">
              <rect x="20" y="20" width="180" height="30" rx="8" class="fill-current opacity-10" />
              <rect x="40" y="60" width="140" height="20" rx="6" class="fill-current opacity-20" />
              <rect x="60" y="90" width="100" height="12" rx="6" class="fill-current opacity-40" />
            </svg>
          </div>
        </article>
      ))}
    </div>
  </div>
</section>

